(ns http-spec.spec-3-files
  (:require [speclj.core :refer :all]))

(describe "Files"

  (xit "/files gives a listing of files in the root directory")
  (xit "/files/img gives a listing of files in the img directory")
  (xit "serves .html files")
  (xit "serves .jpg files")
  (xit "serves .png files")
  (xit "serves .pdf files")

)
