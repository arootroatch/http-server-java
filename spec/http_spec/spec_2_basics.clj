(ns http-spec.spec-2-basics
  (:require [speclj.core :refer :all]))

(describe "Basics"

  (xit "response to '' with index.html")
  (xit "response to '/' with index.html")
  (xit "response to garbage request is 404")
  (xit "contains 'Server' header")

)
